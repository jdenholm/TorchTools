repos:
  - repo : local
    hooks :
      - id: black
        name: black
        entry: black
        language: system
        # always_run: true
        pass_filenames: true
        types: [file, python]
        stages: [commit]